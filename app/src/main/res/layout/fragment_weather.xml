<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@color/weather_area_green"
    android:background="@dimen/space_12">

    <TextView
        android:id="@+id/tvTemp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/label_temp_c"
        android:textColor="@color/weather_text"
        android:textSize="@dimen/text_title"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true" />

    <TextView
        android:id="@+id/tvCity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/label_city"
        android:textColor="@color/weather_text"
        android:textSize="@dimen/text_body"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true" />

    <TextView
        android:id="@+id/tvDesc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/label_desc_cloudy"
        android:textColor="@color/weather_text"
        android:textSize="@dimen/text_body"
        android:layout_below="@id/tvTemp"
        android:layout_alignStart="@id/tvTemp"
        android:layout_marginTop="4dp" />

    <ImageView
        android:id="@+id/imgWeather"
        android:layout_width="@dimen/icon_128"
        android:layout_height="@dimen/icon_128"
        android:src="@drawable/alert_avalanche_danger"
        android:contentDescription="@drawable/ic_cloudy_big"
        android:layout_centerInParent="true" />
</RelativeLayout>
